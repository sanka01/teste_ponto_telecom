<p id="notice"><%= notice %></p>

<h1>Pedidos</h1>
<div class="tabble-wrapper">
  <table class="fl-table">
    <thead>
      <tr>
        <th>N° </th>
        <th>Cliente</th>
        <th>Valor total</th>
        <th>Data</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @pedidos.each do |pedido| %>
        <tr>
          <td><%= pedido.id %></td>
          <td><%= pedido.cliente %></td>
          <td><%= pedido.valor_total %></td>
          <td><%= pedido.created_at.to_date %></td>
          <td><%= link_to 'Exibir', pedido, class: "skewBtn"%></td>
          <td><%= link_to 'Editar', edit_pedido_path(pedido), class: "skewBtn"%></td>
          <td><%= link_to 'Deletar', pedido, method: :delete, data: { confirm: 'Are you sure?' }, class: "skewBtn"%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>

<%= link_to 'Novo Pedido', new_pedido_path, class: "skewBtn"%>
